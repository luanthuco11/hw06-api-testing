# Test-case assets

Each selected endpoint will have separate AI-generated, audited, human-added, and execution-result artifacts. The final workbook will consolidate all endpoint sheets and the test summary.
